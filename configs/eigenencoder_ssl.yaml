# defaults:
#   - override hydra/sweeper: optuna
#   - override hydra/sweeper/sampler: tpe

# hydra:
#   sweeper:
#     sampler:
#       seed: 2137
#       consider_magic_clip: true
#     direction: maximize
#     study_name: minigrid
#     storage: null
#     n_trials: 100
#     n_jobs: 1
#     max_failure_rate: 0.0
#     params:
#       lr: range(1e-4, 1e-3, 1e-4)

# dataset: "yelp"

wandb:
  enabled: false
  project: "eigenencoder-ssl"

meta:
  test: true
  seed: 2136
  num_workers: 2
  save_model_freq: 0 # 0 means no saving
  save_model_path: "models/eigenencoder_ssl_baseline"
  cache_path: null

train:
  epochs: 30
  lr: 5e-4
  lr_scheduler: False
  batch_size: 5
  
gin_encoder:
  hidden_channels: 256
  num_layers: 2
  dropout: 0.5
  emb_style: "none" # []"none", "replace", "concat"]
  emb_dim: 128
  norm: "batch" # []"batch", "layer", "graph", null]

sfcn_encoder:
  channel_number: [28, 58, 128, 256, 512]
  emb_dim: 128
