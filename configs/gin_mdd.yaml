wandb:
  run_name: "pretrain_1024_nofreeze_extradata"
  enabled: true
  project: "gin-mdd"

meta:
  test: true
  seed: 2132

train:
  epochs: 150
  lr: 1e-3
  lr_scheduler: true
  batch_size: 256

classifier:
  linear: false

encoder:
  emb_dim: 1024
  hidden_channels: 2048
  num_layers: 2
  dropout: 0.5
  emb_style: "concat" # []"none", "replace", "concat"]
  norm: "batch" # []"batch", "layer", "graph", null]
  checkpoint_path: "models/barlow_twins_graph/fmri_enc_ep10.pt" # Can be null
  # checkpoint_path: null
  freeze: false